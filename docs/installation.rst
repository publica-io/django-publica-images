============
Installation
============

At the command line::

    $ easy_install django-publica-images

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-images
    $ pip install django-publica-images
